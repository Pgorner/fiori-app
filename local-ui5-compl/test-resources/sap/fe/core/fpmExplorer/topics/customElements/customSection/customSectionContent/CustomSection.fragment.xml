<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m" xmlns:l="sap.ui.layout" xmlns:macros="sap.fe.macros">
	<l:VerticalLayout class="sapUiContentPadding">
		<Text text="The page is in {ui>/editMode} mode" />
		<Text text="The context path name resolves during templating: {contextPath>@sapui.name}" />
		<Text text="Entity property values resolve at runtime. TitleProperty: {TitleProperty}, NameProperty: {NameProperty}" />
		<Text text="Date Time Offset defined in a custom field. In display, time, date and timezone parts can be hidden." />
		<Text text="Here time has been hidden." />
		<macros:Field
			metaPath="dateTimeOffsetVariable"
			change="handlerOnBB.onChange"
			core:require="{handlerOnBB: 'sap/fe/core/fpmExplorer/customSectionContent/CustomSectionNavigation'}"
		>
			<macros:formatOptions showTime="false" />
		</macros:Field>
	</l:VerticalLayout>
</core:FragmentDefinition>
